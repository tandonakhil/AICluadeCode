# Test gate (gate 8) — independent verification run, 2026-08-08

Executed by `test-agent`, independently of `code-agent`'s self-report of 886
tests. All commands run synchronously within this session; no server or
process left running past the turn (verified by `ps aux` after each server
use).

## Corpus verification

### Scenario: corpus-on-disk verify
- Input: `./scripts/seed-data.sh verify`
- Expected: totals matching the corpus of record (25 cases / 179 documents / 644 claims)
- Actual: `public/balanced ok`, `public/probes ok`, `public/risk ok`, `workproduct ok`, `totals: {'cases': 25, 'documents': 179, 'claims': 644}`
- Result: PASS
- Evidence: command output, this session

### Scenario: corpus load into real stores
- Input: `./scripts/seed-data.sh reload`
- Expected: both stores seed cleanly, matching disk totals
- Actual: public store cases=20 documents=153 chunks=418 claims=582 supersessions=2; work-product store cases=5 documents=26 chunks=82 claims=62 supersessions=2; 14 quarantine fixtures classified, 0 disagreeing with §6.4
- Result: PASS
- Evidence: command output, this session

## Suite results (via `tests/run_all.sh`, real exit code captured)

### Scenario: full suite run
- Input: `./tests/run_all.sh` from a clean shell, after corpus reload
- Expected: exit 0, all 8 suites PASS
- Actual: exit code 0; all 8 suites reported PASS (unit, architecture, functional, industry, security, red-team, ui, rendered-ui)
- Result: PASS
- Evidence: full transcript in this session; independently-collected `pytest --collect-only` counts per suite: unit 663 (test_binary_extraction 18, test_boundaries 44, test_comparability 33, test_config 47, test_corpus_loader 48, test_coverage 31, test_enums 45, test_grounding 76, test_harness 12, test_ingest 121, test_model 43, test_pipeline 45, test_schema_gaps 26, test_stores 40, test_utility_name 34), architecture 45, functional 23, industry 26, security 43, red-team 27, ui 48 (test_reachability 27 + test_rendering_invariants 21), rendered-ui 11. Total = 886, matching code-agent's self-report exactly, independently re-derived rather than repeated.

### Scenario: test-count delta vs. previous recorded run
- Input: comparison of my collected counts against `PROJECT_CONTEXT.md`'s Gate 7 pass-3 figures (886 tests: unit 663, architecture 45, functional 23, industry 26, security 43, red-team 27, ui 48, rendered-ui 11)
- Expected: state delta explicitly
- Actual: 0 added, 0 removed, 0 changed since the last recorded run — this is the first `test-agent` execution, so there is no prior test-agent baseline to diff against; the comparison is against code-agent's own last self-report, independently reproduced test-by-test-file.
- Result: PASS (delta = 0, explicitly a same-run re-verification, not a fresh baseline)
- Evidence: counts above

## Spot checks (independent, not from the harness)

### Scenario: RCA-R6 extrapolation trap, live HTTP
- Input: POST /ask, "What ROE has the PA PUC authorized for vertically integrated utilities since 2023?" against the running demo server (RCA_MAX_EVIDENCE_PER_CORPUS=20, real seeded corpus)
- Expected: refusal naming the missing combination, no blended figure, no answer-figures/verbatim table rendered
- Actual: `refusal-statement`: "The corpus contains no case matching market structure × jurisdiction × vintage. The corpus holds cases of each kind separately, and combining them would produce a figure that describes no real proceeding." Coverage panel shows 25 candidates excluded, each on a named dimension (jurisdiction, market structure). No `answer-figures`, `answer-prose` or `verbatim` elements present in the response HTML.
- Result: PASS
- Evidence: `/private/tmp/.../scratchpad/none_comparable.html` (local scratch capture, this session); grep counts recorded in session transcript

### Scenario: grounded question, live HTTP
- Input: POST /ask, "What ROE has the PA PUC authorized in fully-projected-future-test-year distribution cases since 2023?" against the same running demo server
- Expected: real cited answer, sources reflecting what the answer relies on (docket, order date, verbatim quote, locator), not merely retrieval hits
- Actual: answer table with columns Utility/Docket/Order date/Figure/Status/Source; four citation cards each with verbatim quotes ("A return on common equity of 9.60% is just and reasonable" etc.), locator ("p. 2 · Finding of Fact No. 1"), corpus label ("Public — commission docket"); disclosure note states every figure was verified character-for-character against the quoted span before the answer was shown.
- Result: PASS
- Evidence: `/private/tmp/.../scratchpad/grounded_answer.html` (local scratch capture, this session)

### Scenario: ASM-31 — demo safe-minimum guard prevents a refusal from being filed as a grounded screenshot
- Input: start `tools/serve_demo.py` (via uvicorn factory) with `RCA_MAX_EVIDENCE_PER_CORPUS=6` (the product default) against the real seeded corpus
- Expected: server refuses to start rather than allow the grounded demo screen to render as a refusal
- Actual: process exits 1 immediately with: "RCA_MAX_EVIDENCE_PER_CORPUS=6 is below the 12 this demo needs over the corpus of record. At a lower cap the grounded screen renders as a REFUSAL... Failing here rather than filing a refusal screenshot as evidence of a grounded answer."
- Result: PASS
- Evidence: command output, this session (`MIN_EVIDENCE_FOR_DEMO = 12` at `tools/serve_demo.py:221`; product default `RCA_MAX_EVIDENCE_PER_CORPUS=6` at `app/config/settings.py:70`, unchanged)

## Rendered-UI evidence check

### Scenario: screenshots are fresh, from this run, not stale
- Input: `ls -la test-evidence/`, `file test-evidence/03-answer-grounded.png`, visual inspection of `03-answer-grounded.png`
- Expected: timestamps matching this run's `tests/run_all.sh` invocation; real cited content, not placeholder
- Actual: all screenshots timestamped 02:28-02:29 (this session's run, immediately preceding inspection at 02:30); `03-answer-grounded.png` is a real 1440x11094 PNG showing a genuine cited answer (four PA PUC docket citations, verbatim ROE quotes, coverage panel with a 25-case compatibility table)
- Result: PASS
- Evidence: `projects/rate-case-analyzer/test-evidence/03-answer-grounded.png` (viewed directly, this session)

### Scenario: run-report route gap, honestly disclosed
- Input: `test-evidence/11-run-report.txt`
- Expected: gap statement present if no browser route exists
- Actual: file states "NO BROWSER ROUTE SERVES THIS SCREEN" at the top, consistent with the recorded gap (`design-review/11-run-report.html` has no route)
- Result: PASS (honest disclosure confirmed, not a suite failure)
- Evidence: `projects/rate-case-analyzer/test-evidence/11-run-report.txt`

## Coverage-traceability check against 342 acceptance criteria (finding, not a suite failure)

### Scenario: literal AC-ID citation coverage
- Input: grep for `AC-F[0-9]+-[0-9]+` across `knowledge/FUNCTIONAL_SPEC.md` (344 unique IDs found, close to the stated 342) vs. across `dev/tests/` and `dev/app/` combined (123 unique IDs cited)
- Expected: informational — literal citation is not required for coverage, but a large gap is worth surfacing
- Actual: 224 of 344 AC-IDs (~65%) are never cited literally anywhere in `tests/` or `app/`. Spot-checked three of these (AC-F15-01 outcome-completeness gate, AC-F30-0x citation verification, AC-F44-08) against real behavioral tests (`test_ingest.py::test_a_decided_case_with_no_outcome_document_fails_the_gate`, `test_grounding.py::test_a_correct_assertion_verifies`, `test_grounding.py::test_rai_amend_1_span_must_equal_the_stored_claim_quote`) and found real, substantive test coverage for the underlying behavior in each case — the gap is a documentation/traceability gap (tests don't cite the AC-ID in a docstring), not proof of missing functional coverage.
- Result: FINDING, not a failure — recommend `code-agent`/`functional-agent` add AC-ID docstring citations on a future pass so traceability can be verified by grep rather than by spot-check next time. Not blocking.
- Evidence: grep counts, this session; three spot-checked test names above
