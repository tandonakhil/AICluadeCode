# Suite: security (security-architect) — 2026-07-09

Status: **Blocking**
Command: `pytest tests/test_security.py` (run as part of full `pytest` invocation below)
Environment: `dev/backend/.venv` activated, `dev/backend/.env` exported.

### Scenario: Overlong question rejected
- Input: `AskRequest.question` at 2001 characters.
- Expected: 422 validation error (`max_length=2000` enforced).
- Actual: 422 returned.
- Result: PASS
- Evidence: `tests/test_security.py::test_overlong_question_rejected_with_422 PASSED`

### Scenario: Boundary question at exactly 2000 chars accepted
- Input: `AskRequest.question` at exactly 2000 characters.
- Expected: accepted by validation (bound is inclusive), no 422.
- Actual: accepted.
- Result: PASS
- Evidence: `tests/test_security.py::test_boundary_question_at_exactly_2000_is_accepted_by_validation PASSED`

### Scenario: Empty-string question does not crash
- Input: `question=""`
- Expected: clean 422 (regression test for the previously-fixed unhandled 500 — `min_length=1` + `field_validator`).
- Actual: clean 422, no crash.
- Result: PASS
- Evidence: `tests/test_security.py::test_empty_string_question_does_not_crash PASSED`

### Scenario: Whitespace-only question does not crash
- Input: `question="   "`
- Expected: clean 422 (whitespace-stripping validator rejects it before it reaches the LLM call).
- Actual: clean 422, no crash.
- Result: PASS
- Evidence: `tests/test_security.py::test_whitespace_only_question_does_not_crash PASSED`

## Summary
4/4 passed, 0 failed. Not a zero-test suite. Confirms both bugs found during
Phase 4 multi-suite verification (blank/whitespace-input 500, list-content-shape
crash) remain fixed.
