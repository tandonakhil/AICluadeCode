# Test evidence — the ux suite

**Project:** conclave-finance-studio
**Gate:** 8 · Test — pass 22 re-run
**Date:** 2026-08-04
**Commit under test:** `dev` @ **`7757e0d`** · parent repo @ **`299369e`**
**Suite owner (authorship):** `ui-ux-designer`
**Run by:** `test-agent` — this agent **executes and reports** this suite; it does
not author its scenarios.
**Blocking:** yes (project Test Policy: all suites blocking, no advisory exceptions)
**Status:** `EXECUTED`

## Result

**194 scenarios, 194 pass, 0 fail, 0 skip.**

| | |
|---|---|
| entry point | `dev/tests/suites/ux/run.sh` |
| exit code | **0** |
| runner verdict | `EXECUTED — suite passed` |
| scenario files | 3 |

Exit 0 from `_runner.sh` means *executed, all scenarios passed*. Exit 3 would
mean **no scenarios defined** and exit 4 **cannot execute**; neither occurred,
so this is not an empty suite reported as a passing one.

## Test-count delta since the last reported run (`c428fe5`)

| | `c428fe5` | `7757e0d` | Δ |
|---|---|---|---|
| collected | 194 | **194** | **0 — unchanged since `c428fe5`** |

Node-id level: **0 removed**, and no scenario in this suite changed its
assertions in place (AST comparison over both revisions).

---

### Scenario: the suite executes through its own entry point
- Status: EXECUTED
- Input: `dev/tests/suites/ux/run.sh`
- Expected: exit 0, all 194 scenarios pass
- Actual: **exit 0, `EXECUTED — suite passed`, 194 passed**
- Result: PASS
- Evidence: runner transcript; `_runner.sh` lines 26–33 (`NO SCENARIOS DEFINED … exit 3`)

### Scenario: the same 194 scenarios pass under five alternative collection orders
- Status: EXECUTED
- Input: the suite as part of the whole tree, under `file`, `reverse`, `salt8003`, `salt29`, `salt777`
- Expected: no order dependence
- Actual: **3,037 pass in every order**, of which these 194
- Result: PASS
- Evidence: `order-independence-2026-08-04.md`

### Scenario: this suite is reported separately and is never merged into an aggregate
- Status: EXECUTED
- Input: the per-suite breakdown in `PROJECT_CONTEXT.md`'s Test Results section
- Expected: its own line, its own status, its own count
- Actual: **reported separately, `EXECUTED`, blocking**
- Result: PASS
