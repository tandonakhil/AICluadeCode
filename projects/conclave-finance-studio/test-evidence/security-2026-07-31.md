# Test evidence — security suite

**Project:** conclave-finance-studio
**Gate:** 8 · Test (re-run, pass 13 verification)
**Date:** 2026-07-31
**Commit under test:** `dev` @ **`55878c9`** · parent repo @ **`8697994`**
**Owner:** `security-architect` (authored) · executed and reported by `test-agent`
**Blocking:** yes
**Status:** `EXECUTED`
**Entry point:** `dev/tests/suites/security/run.sh`
**Exit code:** 0
**Scenarios: 14 — PASS 14, FAIL 0, SKIP 0**

---

### Scenario: the suite executes end to end
- Status: EXECUTED
- Input: `bash tests/suites/security/run.sh`
- Expected: exit 0
- Actual: `..............  [100%]`, `EXECUTED — suite passed`
- Result: PASS
- Evidence: `EXECUTED — suite passed` / exit 0

### Scenario: the suite is unchanged since the previous run
- Status: EXECUTED
- Input: set-wise node-ID diff `9d819c1` → `55878c9`
- Expected: any delta explained
- Actual: **14 → 14, 0 added, 0 removed, 0 changed**
- Result: PASS (baseline held)
- Evidence: no entries for `tests/suites/security` in either `comm` output

### Scenario: an unauthenticated caller is refused across the real socket
- Status: EXECUTED
- Input: the credential-boundary scenarios, driven as subprocesses
- Expected: 401 without the client token
- Actual: pass
- Result: PASS
- Evidence: included in the 14

### Scenario: the pass-13 changes introduce no new secret or credential surface
- Status: EXECUTED (reviewed against the diff)
- Input: the four pass-13 commits' changed files —
  `backend/ges/warehouse.py`, `backend/app/ui/state.py`,
  `backend/app/ui/routes.py`, `backend/app/evidence/export.py`, and four test files
- Expected: nothing new reads a credential, writes one, or widens the loopback surface
- Actual: none does. The A2 fix adds a 400 on two existing GET routes; the F1 fix
  adds DDL reconciliation inside `seed()`, which is dev/pilot-only and refused
  under `CONCLAVE_ENV=production` via `pilot_transport.seeded_dev_warehouse`; the
  F3 fix is a pure validation function; the F2 fix removes a fixture.
- Result: PASS
- Evidence: `git show --stat 55878c9 38b160d 7f190c9 59351a3`; the 14 scenarios
  pass unchanged

### Scenario: registers 1, 2, 3, 4 and 5 remain open and are claimed nowhere
- Status: EXECUTED
- Input: node-ID scan for `AC-F1-11` and `AC-F1-08`
- Expected: zero claims — mTLS, the DB-level grant, KMS signing, Object-Lock and
  per-skill grants are all still substituted
- Actual: zero claims in this suite. The only node IDs containing either string
  anywhere in the tree are the two parametrize labels on a scenario asserting the
  export is **refused** unless it names the criterion it does not meet.
- Result: PASS
- Evidence: see `register-cross-check-2026-07-31.md`
