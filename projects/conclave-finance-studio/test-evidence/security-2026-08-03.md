# Test evidence — security suite

**Project:** conclave-finance-studio
**Gate:** 8 · Test — re-run after the pass-17 UX redesign
**Date:** 2026-08-03
**Commit under test:** `dev` @ **`6bf8ed9`** · parent repo @ **`5268e9b`**
**Suite owner:** `security-architect`
**Executed by:** `test-agent`
**Blocking:** yes
**Status:** `EXECUTED`
**Entry point:** `dev/tests/suites/security/run.sh`
**Exit code:** 0
**Scenarios: 14 — PASS 14, FAIL 0, SKIP 0**

Test-count delta against `9d605b1`: **0 added, 0 removed, 0 changed.**

---

### Scenario: the suite runs at its own entry point
- Status: EXECUTED
- Input: `dev/tests/suites/security/run.sh`
- Expected: exit 0
- Actual: exit 0
- Result: PASS
- Evidence: `14 passed in 0.72s`; `EXECUTED — suite passed`

### Scenario: the redesign added no credential-bearing surface
- Status: EXECUTED
- Input: the credential and secret hygiene scenarios, re-run over the tree
  including four new UI modules and seven new routes
- Expected: no key material tracked, no `.env` tracked, no journal-submission
  library a dependency, no posting credential resolvable
- Actual: all pass
- Result: PASS
- Evidence: the new object routes (`/evidence/run|agent|dataset`, `/approvals`)
  are read-only renderers over `app/ui/state.py` and introduce no new
  credential path

### Scenario: attributability — a security-relevant consequence of the id gap
- Status: EXECUTED
- Input: the `AC-F5-02`/`-03` finding in `functional-2026-08-03.md`
- Expected: recorded here because attributability is an ITGC/ICFR concern, not
  only a functional one
- Actual: four of five agents that authored findings in the pilot run have no
  principal-registry entry under their author id and are absent from
  `/inventory`; every lineage nonetheless reports `complete=True`
- Result: **advisory — no security scenario asserts this and none fails**, but
  it is routed to `security-architect`, who owns `industry-expert`'s eleven
  compliance obligations including SoD and IPE
- Evidence: see `functional-2026-08-03.md`; reproduced over HTTP as smoke S12
