# Test evidence — red-team suite

**Project:** conclave-finance-studio
**Gate:** 8 · Test (final re-run)
**Date:** 2026-08-02
**Commit under test:** `dev` @ **`9d605b1`** · parent repo @ **`e14c497`**
**Owner:** `responsible-ai-architect` (authored) · executed and reported by `test-agent`
**Blocking:** yes
**Status:** `EXECUTED`
**Entry point:** `dev/tests/suites/red-team/run.sh`
**Exit code:** 0
**Scenarios: 61 — PASS 61, FAIL 0, SKIP 0** (unchanged from the previous run)

This suite is the one this platform's own history says must be **executed** and
not statically reviewed: when it was finally run after a
`STATIC ONLY — NOT EXECUTED` verdict it found three defects a thorough static
review had missed. It was executed here.

---

### Scenario: the suite executes end to end
- Status: EXECUTED
- Input: `bash tests/suites/red-team/run.sh`
- Expected: exit 0, not exit 3 or 4
- Actual: `scenarios: 1 file(s)`, 61 collected, `EXECUTED — suite passed`
- Result: PASS
- Evidence: 61 dots, exit 0

### Scenario: the suite passes under every collection order
- Status: EXECUTED
- Input: the suite as collected inside all eight whole-tree orderings
- Expected: no scenario depends on what ran before it
- Actual: green in all eight, including four separate `reverse` runs
- Result: PASS
- Evidence: see `order-independence-2026-08-02.md`

### Scenario: `AC-REFUSAL-11` is named only to DENY it
- Status: EXECUTED
- Input: every occurrence of `AC-REFUSAL-11` in the suite
- Expected: register 20 and the scope ruling make this a phase-2 criterion; the
  suite may not claim it
- Actual: three occurrences, all in comments/docstrings that state it is **not
  satisfied** — `AC-REFUSAL-11 is NOT VERIFIED.` and `**AC-REFUSAL-11 IS NOT
  SATISFIED BY THIS SCENARIO AND IS NOT SATISFIED …**`
- Result: PASS
- Evidence: zero occurrences in any of the 61 node IDs; zero `COVERS` joins
  naming it

### Scenario: the A20 exposure statement is not widened by anything in this pass
- Status: EXECUTED
- Input: the diff `75f5e27..9d605b1` against the red-team suite
- Expected: the suite is untouched, so its A20 claims are unchanged
- Actual: the suite has **zero** changed lines in this pass's diff
- Result: PASS
- Evidence: `git diff --stat 75f5e27 9d605b1` lists no file under
  `tests/suites/red-team/`

### Scenario: the probe programme's non-attributability set still holds
- Status: EXECUTED
- Input: this pass changed four `AC-F12-15` assertions; `AC-F12-13`/`-14`/`-17`
  are the rest of the non-attributability set
- Expected: strengthening `-15` does not weaken the others
- Actual: all still pass, and the rendered `/review` screen states the
  non-attributability terms in full
- Result: PASS
- Evidence: screenshot `ux-review-desktop-1280-2026-08-02.png` —
  "WHAT PROBE RESULTS ARE NOT USED FOR / Anything about you by name. No
  per-named-person probe score, ranking or trend exists on any management
  surface at any permission level, including administrator…"

### Scenario: register cross-check on this suite's 61 names
- Status: EXECUTED
- Input: all 61 node IDs and every `COVERS` join
- Expected: none of the five declared criteria claimed
- Actual: zero occurrences in node IDs; the only mentions anywhere in the suite
  are the self-denying `AC-REFUSAL-11` comments above
- Result: PASS
- Evidence: see `register-cross-check-2026-08-02.md`
