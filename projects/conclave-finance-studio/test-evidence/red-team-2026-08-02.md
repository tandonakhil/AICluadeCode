# Test evidence — red-team / adversarial suite

**Project:** conclave-finance-studio
**Gate:** 8 · Test (re-run, pass 2)
**Date:** 2026-08-02
**Commit under test:** `dev` @ **`75f5e27`** · parent repo @ **`21af9da`**
**Owner:** `security-architect` / red-team (authored) · executed and reported by `test-agent`
**Blocking:** yes
**Status:** `EXECUTED`
**Entry point:** `dev/tests/suites/red-team/run.sh`
**Exit code:** 0
**Scenarios: 61 — PASS 61, FAIL 0, SKIP 0** (1 scenario file)

This suite is **executed**, not statically reviewed. The distinction is
load-bearing for this suite specifically: the platform's own record is that
when a red-team suite previously reported `STATIC ONLY — NOT EXECUTED` and was
then actually run, it found three defects a thorough static review had missed.

---

### Scenario: the suite executes end to end
- Status: EXECUTED
- Input: `bash tests/suites/red-team/run.sh`
- Expected: exit 0
- Actual: `scenarios: 1 file(s)`, 61 collected, `EXECUTED — suite passed`
- Result: PASS
- Evidence: progress tally `Counter({'.': 61})`, exit 0

### Scenario: the suite passes under every collection order
- Status: EXECUTED
- Input: the suite as collected inside all six whole-tree orderings
- Expected: green in all six
- Actual: green in all six — every scenario of this suite passed in every
  ordering. The single failure of this pass, in the `reverse` ordering, is in
  the **functional** suite, not this one.
- Result: PASS
- Evidence: `order-independence-2026-08-02.md`

### Scenario: the suite leaves the developer's decision ledger untouched
- Status: EXECUTED
- Input: `stat` on `dev/var/broker_db.sqlite3` around the entry point
- Expected: 0 bytes
- Actual: **0**
- Result: PASS
- Evidence: `red-team EXIT=0 ledger_delta=0`

### Scenario: `AC-REFUSAL-11` is still NOT claimed
- Status: EXECUTED
- Input: all 61 node IDs and every `COVERS` docstring
- Expected: zero claims — register 13 holds `AC-REFUSAL-11` NOT VERIFIED, and
  extending the paraphrase battery does not unlock it
- Actual: **zero** node-name claims and **zero** `COVERS` joins. The ID appears
  only in scenarios and comments that state, in capitals, that it is not
  satisfied ("`AC-REFUSAL-11` IS NOT SATISFIED BY THIS SCENARIO AND IS NOT
  SATISFIED…", "WHY `AC-REFUSAL-11` IS NOT VERIFIED. Do not 'fix' this
  scenario.").
- Result: PASS
- Evidence: `register-cross-check-2026-08-02.md`

### Scenario: the new comparator did not open an adversarial path
- Status: EXECUTED
- Input: the whole suite, run against `75f5e27` with `ges/restatement.py` wired
  into `/ges/emit`
- Expected: the new route logic refuses a body naming a comparator-owned field
  with a **named** 403 rather than a generic error, and no scenario regresses
- Actual: 61/61 green; the refusal is
  `restatement_field_not_agent_writable` and is distinct from
  `unknown_emission_field`, which a caller could otherwise use to probe
- Result: PASS
- Evidence: `unit-integration-2026-08-02.md`, "all three agent write paths fail"

---

## Test-count delta

| | Before (`fc197a6`) | After (`75f5e27`) | Delta |
|---|---|---|---|
| red-team | 61 | **61** | — |

**Added 0, removed 0, changed 0.** Node-ID set difference is empty and
`git diff fc197a6..HEAD -- tests/suites/red-team` is empty.
