# Test evidence — industry / compliance suite

**Project:** conclave-finance-studio
**Gate:** 8 · Test (re-run, pass 13 verification)
**Date:** 2026-07-31
**Commit under test:** `dev` @ **`55878c9`** · parent repo @ **`8697994`**
**Owner:** `industry-expert` (authored) · executed and reported by `test-agent`
**Blocking:** yes
**Status:** `EXECUTED`
**Entry point:** `dev/tests/suites/industry/run.sh`
**Exit code:** 0
**Scenarios: 23 — PASS 23, FAIL 0, SKIP 0**

---

### Scenario: the suite executes end to end
- Status: EXECUTED
- Input: `bash tests/suites/industry/run.sh`
- Expected: exit 0
- Actual: `.......................  [100%]`, `EXECUTED — suite passed`
- Result: PASS
- Evidence: `EXECUTED — suite passed` / exit 0

### Scenario: the suite is unchanged since the previous run
- Status: EXECUTED
- Input: set-wise node-ID diff `9d819c1` → `55878c9`
- Expected: any delta explained
- Actual: **23 → 23, 0 added, 0 removed, 0 changed**
- Result: PASS (baseline held)

### Scenario: the seven-year retention obligation is still declared unmet, not claimed
- Status: EXECUTED
- Input: node-ID scan for `AC-F1-08`; the served `/audit/export` screen and the
  auditor export **file**
- Expected: register 4 is open — append-only within the running system,
  hash-chained, with a retention **stamp**, and no retention enforcement
- Actual: no scenario claims the ID. The denial renders **adjacent to the
  retention date** on the screen, and travels inside the export payload the
  auditor consumes with no application login (`AC-F1-04`).
- Result: PASS
- Evidence, from the served pilot: `/audit/export` contains `AC-F1-08`;
  `GET /audit/export/file` returns 200 with `evidence_integrity` present and both
  `AC-F1-08` and `AC-F1-11` inside the payload

### Scenario: the tamper-evidence claim is the permitted one, not a stronger one
- Status: EXECUTED
- Input: the anchor statement rendered on `/audit/export`
- Expected: tamper-**detection** against accidental modification and against a
  party who does not recompute the chain; NOT tamper-evidence against a party
  holding application-level write access (register 3, and the scope ruling's
  permitted wording)
- Actual: exactly that wording, and it names the unmet criterion
- Result: PASS
- Evidence: `"The hash-chain anchor is a labelled digest, not a KMS-signed one.
  An attacker who holds the application could recompute the chain and the
  recomputation would not be detected (AC-F1-11 is unmet)."`

### Scenario: SoD holds on the export path driven end to end
- Status: EXECUTED (on the served pilot)
- Input: as a staff accountant, `POST /proposal/PROP-2026-06-0031/approve`; then
  as controller, the same; then the two-authoriser override; then the export
- Expected: refused on capability, refused on value, then allowed only through a
  recorded override naming two authorisers and a reason code
- Actual: `approve=403` → override `200` → `export=200`
- Result: PASS
- Evidence: `{"approve_status": 403, "override_status": 200, "export_status": 200}`

### Scenario: the export states its authorisation basis on its own face
- Status: EXECUTED (on the served pilot)
- Input: the export screen produced by the drive above
- Expected: `AC-F40-18` — a stored CUEC pass state does not authorise an export;
  this build authorises on a **declared synthetic attestation** and must say so
- Actual: the screen carries `data-authorised-on="synthetic_attestation"`, a
  status kept separate from `no_drift`
- Result: PASS
- Evidence: `<div class="refused" data-testid="cuec-probe-panel" data-authorised-on="synthetic_attestation">`

### Scenario: `AC-F40-17` and `AC-F36-48` are claimed nowhere
- Status: EXECUTED
- Input: node-ID scan plus every textual occurrence
- Expected: registers 27 and 28 keep both open — drift detection does not exist,
  and the abstention band runs on synthetic rather than real close data
- Actual: zero claims. Every mention is a denial, including
  `assert "AC-F40-17 is unmet" in response.text`, which asserts the product tells
  the reader.
- Result: PASS
- Evidence: `AC-F40-17 -> 0`, `AC-F36-48 -> 0` over the 2,664 node IDs;
  `test_abstention.py:311: # AC-F36-48 IS DENIED AND NO SCENARIO IN THIS FILE CLAIMS IT.`
