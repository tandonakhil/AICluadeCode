# Test evidence — UX / accessibility suite (rendered-UI, web backend)

**Project:** conclave-finance-studio
**Gate:** 8 · Test (final re-run)
**Date:** 2026-08-02
**Commit under test:** `dev` @ **`fc197a6`** · parent repo @ **`7ec615a`**
**Owner:** `ui-ux-designer` (authored) · executed and reported by `test-agent`
**Blocking:** yes
**Status:** `EXECUTED`
**Rendering backend:** **Playwright / Chromium** (web).
**Entry point:** `dev/tests/suites/ux/run.sh`
**Exit code:** 0
**Scenarios: 186 — PASS 186, FAIL 0, SKIP 0** (3 scenario files)

**RNTL is not applicable and is not owed here.** MVP1 is **desktop web only**
(PROJECT_CONTEXT.md line 11), there is no React Native surface in this
repository, and a native rendering backend with nothing native to render would
be a suite reporting on something that does not exist. Maestro + simulator
likewise remains unbuilt and unneeded for this project.

Exit 0 is meaningful for this suite specifically: `tests/suites/ux/conftest.py`
exits **4 (CANNOT EXECUTE / STATIC-ONLY)** if Playwright or the Chromium binary
is absent, rather than passing. A real browser launched and a real rendering
engine answered every assertion.

---

### Scenario: the suite executes end to end
- Status: EXECUTED
- Input: `bash tests/suites/ux/run.sh`
- Expected: exit 0, and specifically not exit 4
- Actual: `scenarios: 3 file(s)`, 186 collected, `EXECUTED — suite passed`
- Result: PASS
- Evidence: progress tally `Counter({'.': 186})`, exit 0

### Scenario: the suite passes under every collection order
- Status: EXECUTED
- Input: the suite as collected inside all six whole-tree orderings
- Expected: green in all six — this suite carried two of the order-dependent
  failures being verified (UX-14's persona leak and its override-rate scenario)
- Actual: green in all six
- Result: PASS
- Evidence: `order-independence-2026-08-02.md`

### Scenario: the suite leaves the developer's decision ledger untouched
- Status: EXECUTED
- Input: `stat` on `dev/var/broker_db.sqlite3` before and after the entry point
- Expected: 0 bytes of growth
- Actual: **0**
- Result: PASS
- Evidence: `ux EXIT=0 var_delta=0`

---

## Rendered-UI capture — a real browser against the SERVED pilot

Captured independently of the suite, because the suite renders through
`page.set_content()` over an in-process client. This capture drives **Chromium
over a real socket** against the pilot as found, which is what makes a
screenshot evidence of what a reader meets rather than of what a string
contained.

Four screens × two viewports = **8 captures, 8 clean**. Both viewports are
desktop, which is **complete** rather than partial coverage for this project:
the recorded surface decision is desktop web only, so there is no responsive
requirement to under-serve.

Process lifecycle: the pilot was started, the browser launched, all eight
captures taken, the browser closed and the pilot reaped **inside one command
invocation**, ending with `8021 free — nothing left running`.

### Scenario: every captured screen renders 200 with content
- Status: EXECUTED
- Input: Chromium `page.goto` on `/ask`, `/exceptions`, `/review/ITEM-21400-CP`,
  `/audit/export` at 1280×900 and 1440×1000
- Expected: 200, and a non-trivial rendered tree at both widths
- Expected/Actual per screen (text nodes, identical at both viewports):
  ask 97, exceptions 227, review 132, audit-export 52
- Result: PASS
- Evidence: `ux-ask-desktop-1280-2026-08-02.png`,
  `ux-ask-desktop-1440-2026-08-02.png`,
  `ux-exceptions-desktop-1280-2026-08-02.png`,
  `ux-exceptions-desktop-1440-2026-08-02.png`,
  `ux-review-desktop-1280-2026-08-02.png`,
  `ux-review-desktop-1440-2026-08-02.png`,
  `ux-audit-export-desktop-1280-2026-08-02.png`,
  `ux-audit-export-desktop-1440-2026-08-02.png`

### Scenario: the no-green rule holds under COMPUTED colour
- Status: EXECUTED
- Input: `getComputedStyle` over every rendered element on all eight captures,
  testing resolved `color` and `backgroundColor` for a dominant green channel
- Expected: zero green elements — "green means fine, move on", the exact affect
  a depleted reviewer must not be handed (gate 5)
- Actual: **0 green elements on all eight captures**
- Result: PASS
- Evidence: `green=0` on every line of the capture log. This is the browser's
  resolved colour, not the authored CSS, so a token overridden downstream would
  still be caught.

### Scenario: nothing carrying text is rendered invisible
- Status: EXECUTED
- Input: computed `opacity` on every text-bearing leaf element
- Expected: zero text nodes at `opacity: 0` — this is the compounding-opacity
  class of defect that source greps cannot see
- Actual: **0 on all eight captures**
- Result: PASS
- Evidence: `zero_opacity=0` on every line

### Scenario: the gate-5 reading order survives to the rendered page
- Status: EXECUTED
- Input: visual inspection of `ux-review-desktop-1280-2026-08-02.png`
- Expected: riskiest element first, uncollapsed and largest; the agent's
  narrative collapsed and last; no approve control on Review; six resolution
  types with none pre-selected; a closed rejection list; a required clearing
  period
- Actual: all present as designed — `$918,240.00` renders first in the largest
  type on the screen inside the risk band; "Agent's supporting narrative and
  reasoning trace" is the last element and is collapsed; the screen carries a
  reject control and no approve control; R1–R6 render with none selected; the
  six-reason rejection list has none pre-selected
- Result: PASS
- Evidence: `ux-review-desktop-1280-2026-08-02.png`

---

## Carried advisory — re-verified, still true, still not a failure

- Status: EXECUTED
- Input: computed `font-size` over every text-bearing leaf on all eight captures
- Expected: the previous run raised **10px navigation** and a **10.5–11.5px**
  provenance strip as the smallest text on every screen
- Actual: **minimum computed font size is 10.0px on all four screens at both
  viewports**, unchanged
- Result: **PASS** (no criterion is violated) — carried as an advisory
- Evidence: `min_font=10.0px` on all eight capture lines. No criterion or
  `UX_KB` rule declares an absolute minimum type size — UX-4 / `AC-F41-03` is a
  *relative* check, and it passes — so no suite fails and this does not block.
  Re-raised because the provenance strip is where `AC-F38-11`'s close-clock
  staleness lands, and the reader that criterion exists for is somebody reading
  a figure years later.

## Test-count delta

186 → 186. **0 added, 0 removed, 0 changed** since the previous run
(`dev` @ `55878c9`, 2026-07-31). The rendered captures were regenerated from
scratch at `fc197a6`; the previous run's eight PNGs were deleted rather than
left beside these.
