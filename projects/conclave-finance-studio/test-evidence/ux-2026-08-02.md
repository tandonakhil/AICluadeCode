# Test evidence — UX suite + rendered-UI verification

**Project:** conclave-finance-studio
**Gate:** 8 · Test (final re-run)
**Date:** 2026-08-02
**Commit under test:** `dev` @ **`9d605b1`** · parent repo @ **`e14c497`**
**Owner:** `ui-ux-designer` (authored) · executed and reported by `test-agent`
**Blocking:** yes
**Status:** `EXECUTED`
**Entry point:** `dev/tests/suites/ux/run.sh`
**Exit code:** 0
**Scenarios: 186 — PASS 186, FAIL 0, SKIP 0** (unchanged from the previous run)

**Rendering backend: Playwright/Chromium against the SERVED pilot on 8021** —
not `set_content` over an in-process client. **8 captures, 8 clean.** RNTL is
not applicable: MVP1 is desktop web only and there is no React Native surface,
so two desktop viewports is complete coverage rather than a partial pass.

---

### Scenario: the suite executes end to end
- Status: EXECUTED
- Input: `bash tests/suites/ux/run.sh`
- Expected: exit 0, not exit 3 or 4
- Actual: 186 collected, `EXECUTED — suite passed`
- Result: PASS
- Evidence: 186 dots, exit 0. The suite launches Chromium and its conftest
  exits 4 (STATIC-ONLY) rather than passing if Playwright or the browser binary
  is absent, so exit 0 is positive evidence a rendering engine answered.

### Scenario: the UX half of `AC-F12-15` fires on a real leak
- Status: EXECUTED
- Input: MUTATION M2 (an in-band rate appended to `BAND_STATEMENT`), with
  `test_UX11_AC_F12_15_the_rate_in_force_is_not_readable_from_the_rendered_product`
  reading `locator("main").inner_text()` from a real browser
- Expected: the browser-rendered text catches it
- Actual: FAILED under the mutation, passes without it
- Result: PASS (the assertion fired; the mutation was reverted)
- Evidence: this is the only one of the four sites that reads *rendered* text
  rather than markup, so it is the one that proves the leak is visible to a
  user and not merely present in the source

### Scenario: computed colour — the gate-5 no-green rule
- Status: EXECUTED
- Input: every element on four screens × two viewports, `getComputedStyle` for
  `color`, `backgroundColor`, `borderTopColor`, `borderLeftColor`; flagged when
  green dominates with hue 80–165° and saturation > 0.25
- Expected: zero green elements under **computed** colour, not authored CSS
- Actual: **0 on all 8 captures**
- Result: PASS
- Evidence: `ask 1280 greens=0 · review 1280 greens=0 · exceptions 1280
  greens=0 · audit-export 1280 greens=0` and the same at 1440; 80–360 elements
  inspected per screen

### Scenario: compounded opacity — no text rendered invisible
- Status: EXECUTED
- Input: for every element holding its own text node, the product of its own
  and every ancestor's `opacity`
- Expected: no text at an effective opacity below 0.05 — the compounding-opacity
  defect class that only exists at computed-style level
- Actual: **0 invisible text nodes on all 8 captures**
- Result: PASS
- Evidence: `invisible_text=0` on every capture

### Scenario: the gate-5 reading order on the item screen
- Status: EXECUTED
- Input: `/review/ITEM-21400-CP` at 1280, reading the resolved geometry and
  font size of every landmark
- Expected: riskiest element first and largest; no approve control
- Actual: `risk-band` at top=250 and `riskiest-figure` `$918,240.00` at top=290
  rendered at **42.0px** — the largest text on the screen, against a 20.0px
  `h1` and 15.0px body. Controls present: R1–R6 resolutions and
  "Reject - select a reason first". **No approve control.**
- Result: PASS
- Evidence: `top=  290 DIV riskiest-figure 42.0px $918,240.00`; the queue screen
  likewise renders no approve control

### Scenario: the served pages render as styled pages, not unstyled shells
- Status: EXECUTED
- Input: full-page screenshots at both viewports
- Expected: a laid-out application, not a 200 that renders as a shell
- Actual: sidebar navigation, provenance strip, pilot strip, probe disclosure
  and the six-item queue all laid out
- Result: PASS
- Evidence: `test-evidence/ux-review-desktop-1280-2026-08-02.png` (229,691 bytes),
  and seven more; all eight regenerated this pass

### Scenario: both viewports, per the recorded responsive requirement
- Status: EXECUTED
- Input: 1280 and 1440
- Expected: no desktop-only partial pass
- Actual: identical element counts and identical clean results at both
- Result: PASS
- Evidence: `ask 171 nodes at both · review 80 · exceptions 360 ·
  audit-export 91`

### Scenario: CARRIED ADVISORY — minimum computed font size is 10.0px
- Status: EXECUTED
- Input: the smallest computed `font-size` on any element holding its own text
- Expected: UX-4/`AC-F41-03` is a *relative* check and passes; no rule sets an
  absolute minimum
- Actual: **10.0px** on all four screens at both viewports — unchanged from the
  previous two passes
- Result: PASS (no suite fails), recorded as a carried advisory for
  `ui-ux-designer`
- Evidence: `min_font=10.0px` on all 8 captures

### Scenario: register cross-check on this suite's 186 names
- Status: EXECUTED
- Input: all 186 node IDs and every `COVERS` join
- Expected: none of the five declared criteria claimed
- Actual: zero occurrences
- Result: PASS
- Evidence: see `register-cross-check-2026-08-02.md`
