# Test evidence — the `AC-F41-13` ruling, verified as built

**Project:** conclave-finance-studio
**Gate:** 8 · Test — pass 22 re-run
**Date:** 2026-08-04
**Commit under test:** `dev` @ **`7757e0d`** · parent repo @ **`299369e`**
**Owner:** `test-agent`
**Blocking:** yes
**Status:** `EXECUTED`

Gate 9 blocked on `AC-F41-13`; `functional-design-agent` ruled
(`FUNCTIONAL_SPEC` §28.1–§28.2); passes 21–22 built the ruling. This file
verifies the ruling *as built*, criterion by criterion, whole.

---

## 1 · The retired ID is written nowhere in the tree

### Scenario: `AC-F41-13` appears nowhere in `dev/`, not even in a docstring
- Status: EXECUTED
- Input: `git grep -n "AC-F41-13"` over the whole `dev` tree (product, tests, both suites), plus a filesystem `grep -rn` including untracked files
- Expected: zero hits of any kind — a retired ID in a comment scores as a claim to any by-ID mapper
- Actual: **0 hits, tracked and untracked.** The only occurrences anywhere in the project are in documents that *record the retirement*: `FUNCTIONAL_SPEC.md` (§20 retirement table, §28.1 ruling), `PROJECT_CONTEXT.md`, `pipeline-state.json`
- Result: PASS
- Evidence: `git grep` exit with no output; `grep -rln` over the project names five doc files and no file under `dev/`

### Scenario: no node id and no `COVERS` line names it
- Status: EXECUTED
- Input: all 3,037 collected node ids and all 297 `COVERS` lines in both test trees, scanned for `AC-F41-13` and `AC_F41_13`
- Expected: zero
- Actual: **0 node ids, 0 `COVERS` lines**
- Result: PASS
- Evidence: `forbidden_scan.py` output

### Advisory — two stale references OUTSIDE `dev/`
- Status: EXECUTED
- Input: `grep -rn "AC-F41-13"` across the project's documents
- Expected: references exist only where they record the retirement
- Actual: **two do not.** `knowledge/UX_KB.md:319` still reads *"`AC-F41-13` requires only that approve not be the only…"* in the present tense, and `:549` still lists it as a live binding on the Review screen. `design-review/index.html` lines 878 and 1022 also carry it as a live chip
- Result: **ADVISORY — not a suite failure.** These are `ui-ux-designer`'s artefacts, not checks; §28.1's instruction binds *checks* (*"No ID may name `AC-F41-13` after this pass"*), and no check does. Recorded because a live-tense reference in a KB is how a retired ID gets re-adopted
- Evidence: `knowledge/UX_KB.md:319,549`; `design-review/index.html:878,1022`

---

## 2 · Each replacement criterion's check covers it **whole**

### Scenario: `AC-F41-22` — all three clauses in one scenario, including the absence clause
- Status: EXECUTED
- Input: `backend/tests/test_ui_review.py::TestNoApproveControlHere::test_AC_F41_22_the_three_elements_are_co_visible_and_nothing_here_approves`, read in full and measured under instrumentation
- Expected: (1) evidence set + resolution-type control + structured-reject control co-visible on that screen; (2) none behind a disclosure; (3) **no control that approves anything visible at any permission level**
- Actual: **all three, in one scenario.** Clause 1 asserts `evidence-set`, `resolution-row`, `rejection-reasons` all present; clause 2 asserts `not document.inside_any(testid, "details")` for each; clause 3 asserts `U.approving_controls(document) == []` **and** absence of `approve-lines` **and** absence of `approval-control`, **for both personas** (`staff`, `controller`) — which is what "at any permission level" requires and a single-persona check cannot make. The population is every `/review/ITEM-…` reachable from `/` and awaiting a disposition, not a fixture, guarded by `len(checked) >= 8` and by `seen_eligibility == {"true","false"}`
- Result: PASS
- Actual (instrumented): traversal loop **47 iterations**, per-screen loop entered **14 times**, testid loop 3×14
- Evidence: `loops22.json`; source `test_ui_review.py:136–199`; mutation M8 and rendered-UI scenario "AC-F41-22 in the browser"

### Scenario: `AC-F41-23` — the DETECTION evidence, on the screen that approves, not behind a link
- Status: EXECUTED
- Input: `backend/tests/test_ui_approvals.py::TestTheEvidenceIsOnTheScreenThatApproves::test_AC_F41_23_…`
- Expected: four clauses — on the approve-bearing screen; the detection evidence rather than the journal lines; not behind a disclosure; readable without following a link or leaving the screen
- Actual: **all four.** It compares the approval screen's `evidence-set` cells against a **second render of the finding screen** cell-for-cell (not against the shared model), and asserts they are *not* the journal-lines cells — so "the evidence" cannot be satisfied by the consequence of the decision. Asserted for both personas, skipping the persona for which the approve control does not render, with `carriers` accumulated so the scenario cannot pass by reaching neither
- Result: PASS
- Actual (instrumented): the four loops iterate 2 / 12 / 21 / 5
- Evidence: `loops22.json`; source `test_ui_approvals.py:250–310`

### Scenario: `AC-F41-24` — a non-approving terminal action, no less reachable, not disclosed
- Status: EXECUTED
- Input: `backend/tests/test_ui_approvals.py::TestApproveIsNotTheOnlyVisibleTerminalAction::test_AC_F41_24_…`
- Expected: five clauses — terminal and non-approving; on the same screen in the same state; **no less reachable** than approve; **not behind a disclosure**; and the property itself, at every permission level
- Actual: **all five, each in its own block.** The alternative posts to a different endpoint, with `"approve" not in action` **and** `"override" not in action` (an override is a path *to* approval, and offering it would satisfy the wording while inverting the claim); both controls are asserted not inside `details`, not inside `a`, not inside `dialog`; clause 5 is **computed from the screen** — the set of POST forms carrying a button must exceed `{approve_action}` — so a future control that removed the alternative fails even if the testid survives; `set(carriers) == {"staff","controller"}` forces both levels
- Result: PASS
- Note recorded, not asserted away: the scenario's own docstring discloses that approving is one interaction and rejecting is two (`AC-F41-06` requires a structured reason). The criterion speaks to **reachability**, which is what is asserted; the asymmetry is stated so a reader meets it rather than inferring an equality the scenario does not claim
- Evidence: source `test_ui_approvals.py:348–436`; mutation M8; rendered-UI scenario "AC-F41-24 in the browser" (`approve` at y=1889.7, `other` at y=1968.9, both `opacity 1`, neither in `details`/`dialog`/`a`)

### Scenario: the deliberately-narrowed clause is narrowed where the ruling says, and nowhere else
- Status: EXECUTED
- Input: §28.1's *"Deliberately not carried over: the resolution-type control"* — `AC-F41-13` named it as a fourth element and `-23`/`-24` do not
- Expected: R1–R6 is still bound somewhere, at the point of choice
- Actual: **bound by `AC-F41-22` on the finding screen** (`resolution-row` asserted present and undisclosed), and its anti-default protections still asserted separately by `test_AC_F41_02_no_control_is_preselected_prechecked_or_prefilled` (no `checked`, no pre-filled value, no `sel` class, no `aria-pressed`) and by the `AC-F35-05`/`AC-F35-09` scenarios. **Nothing was dropped; one clause moved screens**
- Result: PASS
- Evidence: `test_ui_review.py:200–215`; rendered-UI reject-form scenario — six radios, `checked=[False]×6`

### Scenario: `AC-F5-08` — listed under the authored identity, each unheld value stated, never blank
- Status: EXECUTED
- Input: `backend/tests/test_ui_governance_screens.py::TestTheInventoryScreen::test_AC_F5_08_an_agent_known_only_by_authorship_is_LISTED_and_says_what_is_missing`, plus the served screen
- Expected: present in the listing under the authored identity; for each of version and entitlements the listing states the value is not recorded **and names the registry gap**; never omitted, blank, dashed, or carrying a neighbour's value
- Actual: **met.** Four agents each have a row; `UNRECORDED_VERSION` / `UNRECORDED_ENTITLEMENTS` are module constants that name both the missing value and the gap; over HTTP the served `/inventory` carries **24** "not recorded" statements and **zero** blank/dash placeholders
- Result: PASS
- Evidence: smoke S36; mutation M9 (blanking `UNRECORDED_VERSION` fails two scenarios)

### Scenario: satisfying `AC-F5-08` is not allowed to imply `AC-F5-07`
- Status: EXECUTED
- Input: the ruling's *"satisfying `AC-F5-08` does not satisfy `AC-F5-07`"*, checked on the built screen
- Expected: the screen itself says so rather than letting a fuller listing imply it
- Actual: **it does.** `inventory-authorship-only-note` renders *"This does NOT satisfy AC-F5-07, which requires each acting agent's REAL version and REAL entitlements: AC-F5-07 is recorded NOT MET for the same cause as AC-F5-02"* — and `AC-F5-07` is claimed by exactly one node id, itself in the `_IS_NOT_MET_` shape
- Result: PASS
- Evidence: smoke S36 (`AC-F5-07` named on the served page); `register-cross-check-2026-08-04.md`
