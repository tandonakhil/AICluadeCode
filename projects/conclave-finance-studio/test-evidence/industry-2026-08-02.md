# Test evidence — industry / compliance suite

**Project:** conclave-finance-studio
**Gate:** 8 · Test (final re-run)
**Date:** 2026-08-02
**Commit under test:** `dev` @ **`9d605b1`** · parent repo @ **`e14c497`**
**Owner:** `industry-expert` (authored) · executed and reported by `test-agent`
**Blocking:** yes
**Status:** `EXECUTED`
**Entry point:** `dev/tests/suites/industry/run.sh`
**Exit code:** 0
**Scenarios: 23 — PASS 23, FAIL 0, SKIP 0** (unchanged from the previous run)

---

### Scenario: the suite executes end to end
- Status: EXECUTED
- Input: `bash tests/suites/industry/run.sh`
- Expected: exit 0, not exit 3 or 4
- Actual: `scenarios: 1 file(s)`, 23 collected, `EXECUTED — suite passed`
- Result: PASS
- Evidence: 23 dots, exit 0

### Scenario: the suite passes under every collection order
- Status: EXECUTED
- Input: the suite as collected inside all eight whole-tree orderings
- Expected: no scenario depends on what ran before it
- Actual: green in all eight
- Result: PASS
- Evidence: see `order-independence-2026-08-02.md`

### Scenario: the two open registers' denials reach the auditor's SCREEN
- Status: EXECUTED
- Input: `GET /audit/export` on the served pilot over HTTP
- Expected: registers 3 and 4 are open, so `AC-F1-11` and `AC-F1-08` must both
  be named as unmet, adjacent to the claims they qualify
- Actual: both present
- Result: PASS
- Evidence: "The hash-chain anchor is a labelled digest, not a KMS-signed one.
  An attacker who holds the application could recompute the chain and the
  recomputation would not be detected (AC-F1-11 is unmet)."

### Scenario: the auditor export FILE carries the integrity statement
- Status: EXECUTED
- Input: `GET /audit/export/file`
- Expected: `AC-F1-04` makes this the artefact an auditor consumes with no
  application login, so the one reader who cannot go and look must be told
- Actual: `status=200 AC-F1-11=True AC-F1-08=True evidence_integrity=True`
- Result: PASS
- Evidence: `{"dossier_count": 6, "dossiers": [{"account_combination": "21400",
  "account_name": "GR/IR Clearing", "amount": "312480.00", …`

### Scenario: the seven-year retention claim prohibition is honoured on the surface
- Status: EXECUTED
- Input: the scope ruling's `AC-F1-08` prohibition — MVP1 must not claim
  seven-year immutable retention anywhere, and wherever a retention date is
  rendered the absence of enforcement must render adjacent to it
- Expected: no unqualified retention claim on any served surface
- Actual: `AC-F1-08` is rendered as an explicitly unmet criterion on both the
  auditor screen and inside the export file
- Result: PASS
- Evidence: `assert stamp["unmet_criterion"] == "AC-F1-08"` in the unit tree,
  and the served `/audit/export` text above

### Scenario: the export authorises on the declared synthetic attestation and says so
- Status: EXECUTED
- Input: the real export path driven over HTTP against the pilot as found
- Expected: `AC-F40-18` — never authorised on a stored CUEC pass state, and
  where authorised on the declared synthetic attestation it must say so
- Actual: `data-authorised-on="synthetic_attestation"`
- Result: PASS
- Evidence: see `smoke-test-2026-08-02.md` S6/S8

### Scenario: ICFR-relevant separation — two DISTINCT authorisers, reason code from a closed list
- Status: EXECUTED
- Input: the override control the denial itself rendered, submitted with
  `authoriser_a=user.a.reyes`, `authoriser_b=user.s.haddad`,
  `reason_code=material_close_deadline`
- Expected: the override is accepted only with two distinct authorisers and a
  reason code drawn from the broker's own closed list
- Actual: 200. An earlier attempt with a reason code **not** on the closed list
  was refused 403 with the list named.
- Result: PASS
- Evidence: `reason code 'approval_value_above_ceiling' is not in the closed
  list ('material_close_deadline', 'known_data_defect_upstream',
  'regulatory_instruction', 'documented_control_exception')` — the refusal
  names what was asked rather than reinterpreting it

### Scenario: register cross-check on this suite's 23 names
- Status: EXECUTED
- Input: all 23 node IDs and every `COVERS` join
- Expected: none of the five declared criteria claimed
- Actual: zero occurrences
- Result: PASS
- Evidence: see `register-cross-check-2026-08-02.md`
