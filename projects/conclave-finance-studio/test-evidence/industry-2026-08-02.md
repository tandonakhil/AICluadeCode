# Test evidence — industry / compliance suite

**Project:** conclave-finance-studio
**Gate:** 8 · Test (re-run, pass 2)
**Date:** 2026-08-02
**Commit under test:** `dev` @ **`75f5e27`** · parent repo @ **`21af9da`**
**Owner:** `industry-sme` (authored) · executed and reported by `test-agent`
**Blocking:** yes
**Status:** `EXECUTED`
**Entry point:** `dev/tests/suites/industry/run.sh`
**Exit code:** 0
**Scenarios: 23 — PASS 23, FAIL 0, SKIP 0** (1 scenario file)

---

### Scenario: the suite executes end to end
- Status: EXECUTED
- Input: `bash tests/suites/industry/run.sh`
- Expected: exit 0, and specifically not exit 3 or 4
- Actual: `scenarios: 1 file(s)`, 23 collected, `EXECUTED — suite passed`
- Result: PASS
- Evidence: progress tally `Counter({'.': 23})`, exit 0

### Scenario: the suite passes under every collection order
- Status: EXECUTED
- Input: all six whole-tree orderings
- Expected: green in all six
- Actual: green in all six — every scenario of this suite passed in every
  ordering. The single failure of this pass, in the `reverse` ordering, is in
  the **functional** suite, not this one.
- Result: PASS
- Evidence: `order-independence-2026-08-02.md`

### Scenario: the suite leaves the developer's decision ledger untouched
- Status: EXECUTED
- Input: `stat` around the entry point
- Expected: 0 bytes
- Actual: **0**
- Result: PASS
- Evidence: `industry EXIT=0 ledger_delta=0`

### Scenario: G-RESTATE is now a control rather than a documented intention
- Status: EXECUTED
- Input: the restatement disclosure this suite depends on — a narrative that
  repeats prior-period narratives above the similarity threshold must carry the
  periods it restates, and the third consecutive restatement must escalate
- Expected: the value is produced by a component the agent cannot influence
- Actual: produced by `ges.restatement.NarrativeComparator`, which runs in the
  GES plane ahead of the broker; every agent path to the field is refused and
  recorded. Before `a1850a5` this was asserted by a comment in
  `context_schema.py` and every G-RESTATE scenario supplied the field itself.
- Result: PASS
- Evidence: `unit-integration-2026-08-02.md`, `AC-F36-33` section

### Scenario: the auditor's artefact still names what is unmet
- Status: EXECUTED
- Input: the smoke test's S10 and S11 against the served pilot
- Expected: `AC-F1-11` and `AC-F1-08` named unmet on the screen **and** inside
  the export file an auditor consumes with no application login (`AC-F1-04`)
- Actual: both present in both places, `evidence_integrity` present in the
  payload
- Result: PASS
- Evidence: `smoke-test-2026-08-02.md` S10, S11

---

## Test-count delta

| | Before (`fc197a6`) | After (`75f5e27`) | Delta |
|---|---|---|---|
| industry | 23 | **23** | — |

**Added 0, removed 0, changed 0.** Node-ID set difference is empty and
`git diff fc197a6..HEAD -- tests/suites/industry` is empty.
