# Test evidence — security suite

**Project:** conclave-finance-studio
**Gate:** 8 · Test (final re-run)
**Date:** 2026-08-02
**Commit under test:** `dev` @ **`9d605b1`** · parent repo @ **`e14c497`**
**Owner:** `security-architect` (authored) · executed and reported by `test-agent`
**Blocking:** yes
**Status:** `EXECUTED`
**Entry point:** `dev/tests/suites/security/run.sh`
**Exit code:** 0
**Scenarios: 14 — PASS 14, FAIL 0, SKIP 0** (unchanged from the previous run)

---

### Scenario: the suite executes end to end
- Status: EXECUTED
- Input: `bash tests/suites/security/run.sh`
- Expected: exit 0, not exit 3 or 4
- Actual: `scenarios: 1 file(s)`, 14 collected, `EXECUTED — suite passed`
- Result: PASS
- Evidence: 14 dots, exit 0

### Scenario: the suite passes under every collection order
- Status: EXECUTED
- Input: the suite as collected inside all eight whole-tree orderings
- Expected: no scenario depends on what ran before it
- Actual: green in all eight
- Result: PASS
- Evidence: see `order-independence-2026-08-02.md`

### Scenario: the api plane holds no credentials, on the served pilot
- Status: EXECUTED
- Input: `GET /health` on the running pilot over HTTP
- Expected: `holds_credentials:false` — the architecture claim, served rather
  than asserted in-process
- Actual: `"holds_credentials":false`
- Result: PASS
- Evidence: `{"status":"ok","env":"pilot","tenant":"tenant-demo","holds_credentials":false,"ges_base_url":"http://127.0.0.1:8022"}`

### Scenario: the new harness code adds no credential or secret surface
- Status: EXECUTED
- Input: the two files added this pass —
  `backend/conclave_harness/rendered_numbers.py` and
  `backend/conclave_harness/live_ledger_guard.py` — read in full
- Expected: no credentials, no network, no writes
- Actual: `rendered_numbers` imports only `decimal`, `re`, `typing`;
  `live_ledger_guard` imports only `os`, `typing`, and `ges.broker.store`
  lazily inside `install()`/`uninstall()`. Neither opens a socket or a file.
- Result: PASS
- Evidence: the guard's only side effect is patching
  `BrokerStore.__init__` in-process, restored by `uninstall()` when the last
  holder lets go

### Scenario: the guard's own failure mode is loud, not silent
- Status: EXECUTED
- Input: `assert_nothing_was_refused` at session teardown
- Expected: a violation swallowed by `raise_server_exceptions=False` still ends
  the run non-zero with its message intact
- Actual: it both raises at the point of construction **and** records, and the
  session-end check fails the run naming the offending test via
  `PYTEST_CURRENT_TEST`
- Result: PASS
- Evidence: proved by MUTATION M3, where the recorded violations surfaced as
  named failures rather than as anonymous 500s

### Scenario: register cross-check on this suite's 14 names
- Status: EXECUTED
- Input: all 14 node IDs and every `COVERS` join
- Expected: none of the five declared criteria claimed
- Actual: zero occurrences
- Result: PASS
- Evidence: see `register-cross-check-2026-08-02.md`
