# Test evidence — security suite

**Project:** conclave-finance-studio
**Gate:** 8 · Test (re-run, pass 2)
**Date:** 2026-08-02
**Commit under test:** `dev` @ **`75f5e27`** · parent repo @ **`21af9da`**
**Owner:** `security-architect` (authored) · executed and reported by `test-agent`
**Blocking:** yes
**Status:** `EXECUTED`
**Entry point:** `dev/tests/suites/security/run.sh`
**Exit code:** 0
**Scenarios: 14 — PASS 14, FAIL 0, SKIP 0** (1 scenario file)

14 is a small suite. It is not a *zero* suite, and it is reported with its
count rather than as a bare "0 failed".

---

### Scenario: the suite executes end to end
- Status: EXECUTED
- Input: `bash tests/suites/security/run.sh`
- Expected: exit 0, and specifically not exit 3 or 4
- Actual: `scenarios: 1 file(s)`, 14 collected, `EXECUTED — suite passed`
- Result: PASS
- Evidence: progress tally `Counter({'.': 14})`, exit 0

### Scenario: the suite passes under every collection order
- Status: EXECUTED
- Input: all six whole-tree orderings
- Expected: green in all six
- Actual: green in all six — every scenario of this suite passed in every
  ordering. The single failure of this pass, in the `reverse` ordering, is in
  the **functional** suite, not this one.
- Result: PASS
- Evidence: `order-independence-2026-08-02.md`

### Scenario: the suite leaves the developer's decision ledger untouched
- Status: EXECUTED
- Input: `stat` around the entry point
- Expected: 0 bytes
- Actual: **0**
- Result: PASS
- Evidence: `security EXIT=0 ledger_delta=0`

### Scenario: the new comparator's refusal is a named refusal, not a leak
- Status: EXECUTED
- Input: the HTTP write path added at `a1850a5` — `/ges/emit` refuses a body
  naming `restates_periods` or `restates_consecutive_periods` with 403 and
  `reason_code = restatement_field_not_agent_writable`
- Expected: distinct from `unknown_emission_field` (400), so a caller cannot use
  the generic error to discover which fields exist, and a control event is
  recorded before the refusal is raised
- Actual: distinct, asserted by
  `test_AC_F36_33_a_supplied_restatement_state_is_not_reported_as_an_unknown_field`;
  the event is emitted **first**, so a caller that swallows the exception has
  still been observed
- Result: PASS
- Evidence: `unit-integration-2026-08-02.md`, mutation M7 (control event
  removed → 6 scenarios fail)

### Scenario: `SECURITY_KB` §6 T1 — the threshold that gets widened
- Status: EXECUTED
- Input: `ges/restatement.py`'s stopword list and similarity threshold
- Expected: a fixed, small stopword set and a strict `>` comparison, with the
  strictness asserted rather than assumed
- Actual: `test_exceeding_the_threshold_is_strict` asserts a narrative scoring
  exactly the threshold has **not** exceeded it
- Result: PASS
- Evidence: `DEFAULT_SIMILARITY_THRESHOLD = 0.6`, 13 fixed stopwords

---

## Test-count delta

| | Before (`fc197a6`) | After (`75f5e27`) | Delta |
|---|---|---|---|
| security | 14 | **14** | — |

**Added 0, removed 0, changed 0.** Node-ID set difference is empty and
`git diff fc197a6..HEAD -- tests/suites/security` is empty.
