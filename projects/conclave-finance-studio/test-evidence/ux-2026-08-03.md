# Test evidence — ux suite

**Project:** conclave-finance-studio
**Gate:** 8 · Test — re-run after the pass-17 UX redesign
**Date:** 2026-08-03
**Commit under test:** `dev` @ **`6bf8ed9`** · parent repo @ **`5268e9b`**
**Suite owner:** `ui-ux-designer`
**Executed by:** `test-agent`
**Blocking:** yes
**Status:** `EXECUTED`
**Entry point:** `dev/tests/suites/ux/run.sh`
**Exit code:** 0
**Scenarios: 194 — PASS 194, FAIL 0, SKIP 0**

Exit 0 is positive evidence a rendering engine answered: the suite's conftest
calls `pytest.exit(returncode=4)` — STATIC-ONLY — if Playwright or its browser
binary is absent. Chromium **148.0.7778.96** was verified present independently.

Test-count delta against `9d605b1`: **+12 added, −4 removed, 8 net.**
Added: 7 in `test_ux_accessibility.py` (the run report entering the screen
matrix, four scenarios × two themes) and 5 in `test_ux_journey.py` (the
readiness journey splitting into a per-agent walk). Removed: the four
`/exceptions`- and navigation-worded node IDs, each with a named successor —
see `unit-integration-2026-08-03.md` rows 16-19.

---

### Scenario: the suite runs at its own entry point
- Status: EXECUTED
- Input: `dev/tests/suites/ux/run.sh`
- Expected: exit 0, and NOT exit 4
- Actual: exit 0
- Result: PASS
- Evidence: `194 passed in 28.65s`; `EXECUTED — suite passed`

### Scenario: the readiness journey is now a per-agent walk
- Status: EXECUTED
- Input: `TestTheReadinessJourney`, rewritten at pass 17
- Expected: the removed "readiness is reachable from the navigation on every
  screen" is replaced by something stronger, not merely deleted
- Actual: replaced by **two** scenarios —
  `test_every_agent_has_its_own_readiness_address` and
  `test_readiness_is_reached_from_the_agent_it_is_a_property_of`
- Result: PASS
- Evidence: the old build could not satisfy the new claim for four of five
  agents, because `/readiness` was hard-bound to one skill

### Scenario: UX-13's staff-accountant night starts on the queue
- Status: EXECUTED
- Input: `TestUX13StaffAccountantNight`
- Expected: `/` is the queue and Ask is one click from it
- Actual: passes
- Result: PASS
- Evidence: independently re-confirmed over HTTP — smoke S4

### Scenario: rendered-UI verification is reported separately, not folded in here
- Status: EXECUTED
- Input: this agent's own Playwright run against the SERVED pilot on 8021
- Expected: reported as its own evidence rather than merged into this suite's
  count
- Actual: 8 scenarios, 8 pass, 13 screenshots
- Result: PASS
- Evidence: `rendered-ui-2026-08-03.md`. It found a layout defect this
  194-scenario suite does not catch — see that file, R9.
