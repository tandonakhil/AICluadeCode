export default function ChatPage() {
  return (
    <main>
      <h1>{"{{PROJECT_NAME}}"}</h1>
      <p>Placeholder chat UI — ui-ux-designer replaces this at the Experience Design gate.</p>
    </main>
  );
}
